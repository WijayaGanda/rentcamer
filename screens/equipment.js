import React from "react";
import { Center, Text } from "native-base";

const Equipment = ({route}) => {
    // const { item } = route.params;  // Mengambil data item yang dikirimkan
    return (
      <Text>Hello</Text> 
    )
};

export default Equipment;